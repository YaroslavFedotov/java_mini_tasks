import java.util.*;

public class Line {
   public static String WhoIsNext(String[] names, int n)
     {
        var customer = "";
        ArrayList<String> people = ArrayList(Collection <? extends E> names);
        people.get(0)
        return names[0];
     }
   
}